/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enables statically exporting the website if desired later, but App Router handles static optimization natively.
  reactStrictMode: true,
};

module.exports = nextConfig;
